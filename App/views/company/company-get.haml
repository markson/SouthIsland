%table.table.table-hover
  %thead
    %tr
      %th{colspan:'2'}= @company.name
  %tbody
    %tr
      %th Description
      %td=@company.description
    - @activities.each_with_index do |activity, index|
      %tr
        -if index == 0
          %th{rowspan:@activities.count} Activities
        %td
          %a{href:"/activity/#{activity.id}"}= activity.name

